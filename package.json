{
  "name": "program-registration-schema",
  "version": "1.0.0",
  "description": "A generic, reusable data model for multi-track program registration systems (e.g. career accelerators, cohort programs, internship pipelines) built with Drizzle ORM and PostgreSQL.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "generate": "drizzle-kit generate",
    "migrate": "tsx src/db/migrate.ts",
    "seed": "tsx src/db/seed.ts",
    "studio": "drizzle-kit studio"
  },
  "dependencies": {
    "drizzle-orm": "^0.36.0",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.11.0",
    "drizzle-kit": "^0.28.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
